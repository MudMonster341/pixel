// Every item in the game. `frame` is the icon's position in assets/items.png.
// `maxStack` is how many fit in one inventory slot.
const ITEMS = {
  apple: { name: 'Apple', frame: 0, maxStack: 5 },
  potion: { name: 'Health Potion', frame: 1, maxStack: 3 },
  gem: { name: 'Blue Gem', frame: 2, maxStack: 9 },
  sword: { name: 'Old Sword', frame: 3, maxStack: 1 },
};
